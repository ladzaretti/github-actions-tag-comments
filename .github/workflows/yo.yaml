on:
  release:
    types: [published]

env:
  # renovate: datasource=docker depName=ghcr.io/renovatebot/renovate versioning=docker
  RENOVATE_SLIM_VERSION: 44.14.12
  # renovate: datasource=docker depName=ghcr.io/renovatebot/renovate versioning=docker
  RENOVATE_FULL_VERSION: 44.14.12-full
