on:
  release:
    types: [published]

env:
  # renovate: datasource=docker depName=ghcr.io/renovatebot/renovate versioning=docker
  RENOVATE_SLIM_VERSION: 37.137.3-slim
  # renovate: datasource=docker depName=ghcr.io/renovatebot/renovate versioning=docker
  RENOVATE_FULL_VERSION: 37.137.3-full
